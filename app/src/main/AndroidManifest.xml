<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.yssz.xiangxuedemo">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />

    <application
        android:name=".base.MyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">
        <activity android:name=".workmanager.DingDingActivity"></activity>
        <activity android:name=".ui.WorkManagerActivity" />
        <activity android:name=".ui.ViewModelActivity" />
        <activity
            android:name=".ui.NewsListActivity"
            android:label="@string/title_activity_drawer_layout_activiy"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".ui.CollapsingActivity"
            android:theme="@style/CollapsingActivityTheme" />
        <activity android:name=".ui.RetrofitActivity" />
        <activity android:name=".ui.WebViewActivity" />
        <activity android:name=".ui.HttpUrlConnectionAct" />
        <activity android:name=".ui.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>